# 온라인 빙고 게임 설계서

## 1. 목표와 범위

로그인 화면 없이 닉네임, 방 이름, 비밀번호만으로 입장하는 실시간 웹 빙고 게임을 만든다.

- 프론트엔드: 정적 웹 앱(GitHub Pages 또는 Firebase Hosting)
- UI 스택: React + TypeScript + Vite + Tailwind CSS + shadcn/ui
- 실시간 상태: Firebase Realtime Database
- 사용자 식별: Firebase Anonymous Authentication
- 별도 서버 및 Cloud Functions: MVP에서는 사용하지 않음
- 게임 성격: 지인끼리 플레이하는 캐주얼 게임

Anonymous Authentication은 화면에 로그인 절차를 만들지 않고 브라우저별 UID만 발급받는 용도다.

## 2. 핵심 설계 결정

1. 방 만들기와 입장 모두 `플레이어 닉네임`을 추가로 입력한다.
2. 비밀번호 원문은 데이터베이스에 저장하지 않는다.
3. 방 이름과 비밀번호로 내부 방 키를 계산한다. 잘못 입력하면 다른 키가 계산되므로 방을 찾을 수 없다.
4. 빙고판 작성이 시작되면 신규 입장을 막는다. 기존 참가자의 재접속만 허용한다.
5. 모든 참가자가 준비되면 3초 카운트다운 후 자동으로 게임을 시작한다.
6. 채팅과 단어 부르기는 같은 목록에 표시하되 입력 동작은 분리한다. 실수로 채팅이 턴 사용으로 처리되는 것을 막기 위해서다.
7. 완성자의 관전과 판 공개 기능을 포함한다. 최대 5×5 텍스트 판은 데이터가 작아 소규모 방에서는 부담이 미미하다.
8. 최대 인원 설정을 포함한다. 범위는 2~20명, 기본값은 8명이다.

## 3. 화면 흐름

### 3.0 UI 구성 원칙

UI는 `shadcn/ui` 컴포넌트를 기준으로 구현한다. 별도 UI 프레임워크를 추가하지 않고 필요한 컴포넌트만 설치한다.

- 버튼과 주요 동작: `Button`
- 폼 입력: `Input`, `Label`, `Textarea`
- 칸 수 선택: `Slider`와 현재 값 표시
- 모드 선택: `RadioGroup`
- 준비 완료: `Switch` 또는 토글형 `Button`
- 방/플레이어 영역: `Card`, `Avatar`, `Badge`
- 취소·초기화 확인: `AlertDialog`
- 모바일 패널과 참가자 목록: `Sheet`, `ScrollArea`
- 결과와 설정 구분: `Tabs`
- 오류 및 완료 알림: `Sonner`

빙고판은 shadcn 컴포넌트로 억지로 구성하지 않고 CSS Grid와 기본 버튼 요소를 사용한다. 화면은 모바일 우선 반응형으로 만들며, 모바일에서는 채팅과 참가자 목록을 `Sheet`로 연다.

### 3.1 홈

버튼은 두 개다.

- 방 만들기
- 방 들어가기

#### 방 만들기 폼

- 내 닉네임: 1~12자
- 방 이름: 1~30자
- 방 비밀번호: 4~30자
- `방 만들기` 버튼

같은 방 이름과 비밀번호 조합의 활성 방이 이미 있으면 생성을 거절한다.

#### 방 들어가기 폼

- 내 닉네임: 1~12자
- 방 이름
- 방 비밀번호
- `입장하기` 버튼

실패 메시지는 보안을 위해 `방이 없거나 비밀번호가 올바르지 않습니다`로 통합한다.

### 3.2 방 설정 (`SETUP`)

모든 참가자가 볼 수 있는 정보:

- 방 이름
- 방장 표시
- 현재 참가자 목록 및 접속 상태
- 현재 설정값
- 채팅

방장만 변경할 수 있는 값:

- 한 줄 칸 수: 슬라이더 2~5
- 주제: 표시용 문자열, 1~40자
- 목표 빙고 수: 숫자 입력
- 승리 조건: `한 명 완성` / `한 명 빼고`
- 완성 판정: `엄격` / `느슨`
- 최대 인원: 2~20명, 기본 8명
- `빙고판 채우기 시작` 버튼

목표 빙고 수의 허용 범위는 `1 ~ (2 × 칸 수 + 2)`다. N×N 판에서 가로 N줄, 세로 N줄, 대각선 2줄이 최대이기 때문이다.

현재 참가자 수보다 작은 최대 인원으로 변경할 수 없다.

### 3.3 빙고판 작성 (`FILLING`)

방장이 시작하면 설정을 잠그고 각 참가자에게 N×N 입력판을 표시한다.

- 정확히 N²개의 단어를 입력해야 한다.
- 빈 칸은 허용하지 않는다.
- 한 칸은 1~30자로 제한한다.
- 자기 판 안에서 정규화 결과가 같은 단어는 중복 입력할 수 없다.
- 다른 사람의 작성 내용은 게임 중 보이지 않는다.
- `준비 완료`는 토글 버튼이다.
- 준비 완료를 해제하면 다시 편집할 수 있다.
- 준비 완료 상태에서는 자기 판을 편집할 수 없다.

모든 참가자가 준비 완료가 되면 3초 카운트다운 후 `PLAYING`으로 이동한다. 카운트다운 중 누군가 준비를 해제하면 카운트다운을 취소한다.

방장은 이 단계에서도 `방 설정으로 돌아가기`를 사용할 수 있다. 확인 창을 거친 뒤 모든 판과 준비 상태를 초기화한다.

### 3.4 게임 (`PLAYING`)

화면 구성:

- 상단: 주제, 목표 빙고 수, 현재 턴
- 참가자 목록: 방장, 접속 상태, `N빙고!`, 완성/관전 상태
- 중앙: 내 빙고판
- 우측 또는 하단: 불린 단어 기록과 채팅
- 방장 전용: `턴 넘기기`, `방 설정으로 돌아가기`

턴 순서는 게임 시작 시 참가자 순서를 한 번 섞어 정한다. 한 턴에 단어 하나를 부르면 다음 활성 참가자에게 자동으로 넘어간다.

입력 동작:

- `채팅 보내기`: 누구나 언제든 사용하며 턴에 영향을 주지 않는다.
- `단어 부르기`: 현재 턴인 참가자에게만 활성화된다.

두 동작은 같은 타임라인에 표시하되, 단어 호출은 색상과 아이콘으로 구분한다. 이미 불린 단어는 다시 부를 수 없다.

턴 제한시간은 MVP에서 두지 않는다. 참가자가 자리를 비우거나 연결이 끊겼을 때 방장이 `턴 넘기기`를 사용할 수 있다.

### 3.5 게임 종료 (`FINISHED`)

표시 내용:

- 결과 및 순위
- 각 참가자의 최종 빙고 수
- 완성한 참가자의 공개 판
- 종료 시점에 남아 있던 참가자들의 판과 현재 상태
- 채팅
- 방장 전용 `다시하기` 버튼

`다시하기`는 `SETUP`으로 돌아가며 다음 내용을 초기화한다.

- 빙고판
- 체크 상태
- 준비 상태
- 호출 단어 기록
- 턴 순서
- 빙고 수와 순위

닉네임, 참가자 목록, 일반 채팅은 유지한다. 일반 채팅은 최근 100개만 보관한다.

## 4. 게임 규칙

### 4.1 단어 정규화

엄격 모드의 동일 단어 판정과 중복 검사는 다음 순서로 정규화한 값으로 비교한다.

1. 앞뒤 공백 제거
2. 연속된 공백을 한 칸으로 변경
3. 영문 대소문자 무시
4. Unicode NFC 정규화

문장부호는 자동 제거하지 않는다. 예를 들어 `사과!`와 `사과`는 다른 값이다. 화면에는 사용자가 입력한 원문을 보여준다.

### 4.2 엄격 모드

- 참가자는 게임 중 판을 직접 체크할 수 없다.
- 호출된 단어의 정규화 값이 자기 칸과 같으면 해당 칸을 자동 체크한다.
- 체크는 해제되지 않는다.
- 호출되지 않은 단어는 체크할 수 없다.

여기서 사용자가 말한 `알아서 지워준다`는 해당 칸을 삭제하는 것이 아니라 완료 표시로 자동 체크하는 것으로 해석한다.

### 4.3 느슨 모드

- 호출 단어와 칸의 일치 여부를 시스템이 판단하지 않는다.
- 참가자가 자기 판의 칸을 직접 체크하거나 해제한다.
- 다른 참가자의 판은 수정할 수 없다.
- 목표 빙고 수를 달성해 완성 처리된 뒤에는 체크 상태를 잠근다.

### 4.4 빙고 수 계산

현재 완성된 가로, 세로, 두 대각선의 합을 표시한다.

- 4×4 예시: 최대 10빙고
- 표시 예시: `0빙고`, `1빙고!`, `2빙고!`
- 느슨 모드에서 체크를 해제해 줄이 깨지면 완성 전까지 숫자도 감소한다.
- 목표 빙고 수를 최초로 달성하면 완료 시각과 순위를 고정한다.

### 4.5 승리 조건

#### 한 명 완성

어느 한 참가자라도 목표 빙고 수에 도달하는 즉시 게임을 종료한다. 해당 참가자가 승자다.

같은 호출 처리에서 여러 명이 동시에 목표에 도달하면 공동 승리로 처리한다.

#### 한 명 빼고

목표 빙고 수를 달성한 참가자는 순위가 고정되고 관전 상태로 전환된다. 완성자는 이후 턴 순서에서 제외한다.

참가자 수가 P명일 때 P-1명이 완료하면 게임을 종료한다. 마지막 미완성 참가자는 최종 순위가 된다. 2명 방에서는 한 명이 완료하면 종료되므로 `한 명 완성`과 결과가 같다.

## 5. 관전 및 판 공개

관전 기능은 포함한다.

- `한 명 빼고`에서 완료자는 자기 판을 더 이상 수정하지 못하고 남은 게임을 관전한다.
- 완료자의 판은 완료 즉시 모든 참가자에게 공개한다.
- 게임 종료 시 모든 참가자의 최종 판을 공개한다.
- 플레이 중 미완성 참가자의 판은 UI에서 공개하지 않는다.

N≤5, 최대 20명일 때 판은 최대 500칸의 짧은 텍스트이므로 용량 자체는 작다. 트래픽을 줄이기 위해 방 전체를 매번 다시 구독하지 않고 판, 채팅, 호출 기록, 참가자 요약을 각각 구독한다.

## 6. 권한

| 기능 | 방장 | 참가자 | 관전자/완성자 |
|---|---:|---:|---:|
| 설정 변경 | O | X | X |
| 빙고판 작성 | O | O | X |
| 준비 토글 | O | O | X |
| 단어 부르기 | 자기 턴만 | 자기 턴만 | X |
| 자기 판 체크(느슨) | O | O | X |
| 채팅 | O | O | O |
| 턴 넘기기 | O | X | 방장이면 O |
| 설정으로 돌아가기 | O | X | 방장이면 O |
| 다시하기 | O | X | 방장이면 O |

방장이 `방 설정으로 돌아가기`를 누르면 확인 문구를 표시한다.

> 진행 중인 빙고가 취소되고 모든 판과 진행 상황이 삭제됩니다. 계속할까요?

## 7. 접속 및 이탈 처리

- Firebase의 연결 상태와 `onDisconnect`를 이용해 접속 여부를 표시한다.
- 일시적으로 끊긴 참가자의 자리는 유지한다.
- 같은 브라우저에서 돌아오면 Anonymous UID로 자기 자리와 판을 복구한다.
- 다른 브라우저나 시크릿 창에서는 같은 사람으로 자동 복구할 수 없다.
- 게임 시작 뒤에는 신규 참가자 입장을 막고 기존 UID의 재접속만 허용한다.
- 연결이 끊긴 참가자의 턴은 방장이 넘길 수 있다.

방장이 나가면 게임을 즉시 삭제하지 않는다. 60초 동안 재접속을 기다리고, 돌아오지 않으면 접속 중인 참가자 중 가장 먼저 입장한 사람에게 방장 권한을 이전한다. 권한 이전은 Firebase transaction으로 한 번만 성공하게 한다.

방 설정 단계에서는 방장이 연결이 끊긴 참가자를 목록에서 제거할 수 있다.

## 8. 방 비밀번호 처리

MVP는 서버 함수를 쓰지 않으므로 비밀번호를 일반 서비스 수준으로 안전하게 검증할 수는 없다. 캐주얼 방 입장 장벽으로 다음 방식을 사용한다.

```text
roomKey = SHA-256(normalizedRoomName + "\0" + password)
```

- 비밀번호 원문과 해시는 DB 필드에 저장하지 않는다.
- 방 목록 조회를 허용하지 않는다.
- roomKey를 아는 사용자만 해당 경로에 접근한다.
- 잘못된 비밀번호는 존재하지 않는 경로를 가리킨다.

이 구조는 지인용 게임에는 충분하지만 악의적인 사용자를 막는 강한 인증은 아니다. 공개 서비스에서 안전한 비밀번호 검증과 치트 방지가 필요해지면 신뢰할 수 있는 서버 또는 Cloud Function이 필요하다.

## 9. Firebase 데이터 구조

배열보다 객체 키를 주로 사용해 일부 데이터만 구독하고 갱신한다.

```text
rooms/{roomKey}/
  meta/
    displayName
    hostUid
    phase                 SETUP | FILLING | COUNTDOWN | PLAYING | FINISHED
    locked
    createdAt
    updatedAt
    expiresAt

  settings/
    size                  2..5
    topic
    targetBingos
    victoryMode           FIRST | ALL_BUT_ONE
    markingMode           STRICT | LOOSE
    maxPlayers            2..20

  players/{uid}/
    nickname
    joinedAt
    ready
    status                ACTIVE | COMPLETED
    bingoCount
    completedAt
    rank

  presence/{uid}/
    connectedAt

  boards/{uid}/
    size
    cells/{index}
      text
      normalized
      marked
    locked

  revealedBoards/{uid}/
    cells/{index}
    bingoCount
    revealedAt

  round/
    id
    turnOrder/{index}
    currentTurnIndex
    startedAt
    finishedAt
    result

  calls/{callId}/
    roundId
    callerUid
    text
    normalized
    createdAt

  messages/{messageId}/
    uid
    nickname
    type                  CHAT | CALL | SYSTEM
    text
    createdAt
```

`CALL`은 호출 기록의 표시용 복사본이다. 실제 게임 판정은 `calls`를 기준으로 한다.

## 10. 원자적으로 처리할 작업

동시 입력으로 상태가 어긋나지 않도록 다음 작업은 Firebase transaction 또는 하나의 multi-location update로 처리한다.

- 방 생성
- 정원 확인과 입장
- 마지막 준비 완료와 카운트다운 전환
- 단어 호출, 중복 확인, 다음 턴 이동
- 완료 순위 부여
- 승리 조건 확인과 게임 종료
- 방장 권한 이전
- 설정 화면 복귀와 라운드 초기화

모든 시간 기록은 기기 시간이 아니라 Firebase 서버 타임스탬프를 사용한다.

## 11. 데이터 사용량 관리

- 방 루트 전체에 실시간 리스너를 걸지 않는다.
- 참가자 요약, 내 판, 공개 판, 호출 기록, 채팅을 별도로 구독한다.
- 채팅은 최근 100개만 조회한다.
- 호출 기록은 현재 라운드 것만 조회한다.
- 판 셀 하나를 체크할 때 해당 셀과 빙고 수만 갱신한다.
- 종료 화면은 이미 저장된 최종 판을 재사용한다.

이 범위라면 관전 기능보다 연결된 브라우저 수와 무제한 채팅 누적이 더 큰 비용 요인이다. 최대 인원과 채팅 제한으로 충분히 제어할 수 있다.

## 12. 방 수명

- 방장은 `방 닫기`로 방 데이터를 삭제할 수 있다.
- 활동이 있을 때 `expiresAt`을 갱신한다.
- 클라이언트는 만료된 방을 입장 불가로 처리한다.
- 같은 방 이름과 비밀번호로 새 방을 만들 때 기존 방이 만료됐다면 덮어쓸 수 있다.

MVP에는 예약 삭제 서버가 없으므로 비정상 종료된 오래된 데이터가 즉시 삭제되지는 않는다. 실제 사용량이 문제가 될 때만 예약 Cloud Function을 추가한다.

## 13. 필수 검증 항목

- 닉네임 중복은 대소문자와 공백 정규화 후 금지
- 설정 범위와 목표 빙고 수 범위 검사
- 방 정원 초과 입장 방지
- 잠긴 방 신규 입장 방지
- 자기 턴이 아닌 호출 거절
- 같은 단어 재호출 거절
- 엄격 모드 직접 체크 거절
- 느슨 모드 타인 판 수정 거절
- 완성 후 판 수정 거절
- 방장 전용 작업 거절
- 이전 라운드의 늦은 이벤트 무시(`roundId` 비교)

## 14. MVP에서 제외할 기능

- 회원가입과 계정 복구
- 공개 방 목록과 검색
- 음성 인식
- 턴 제한시간
- AI 단어 판정 및 유사어 판정
- 강한 치트 방지
- 전적 및 장기 랭킹
- 예약 서버에 의한 오래된 방 자동 삭제

필요성이 확인되기 전에는 위 기능을 추가하지 않는다.

## 15. 구현 순서

1. 홈, 방 생성/입장, Anonymous Auth
2. 방 설정과 참가자/presence 목록
3. 빙고판 작성과 준비 상태
4. 턴, 호출 기록, 채팅
5. 엄격/느슨 체크와 빙고 계산
6. 두 승리 조건과 종료 화면
7. 관전, 재시작, 설정 복귀
8. Firebase Security Rules와 재접속/동시성 검증

## 16. 완료 기준

- 두 브라우저 이상에서 같은 방에 입장하고 참가자 목록이 실시간으로 보인다.
- 방장만 설정을 바꾸고 작성 단계를 시작할 수 있다.
- 모든 참가자가 준비되기 전에는 게임이 시작되지 않는다.
- 호출과 체크가 모드에 맞게 동작하고 모든 화면에 동기화된다.
- 빙고 수와 승리 조건이 정확히 계산된다.
- 완성자는 관전하고 판을 공개할 수 있다.
- 방장의 설정 복귀와 다시하기가 라운드 상태를 완전히 초기화한다.
- 새로고침 후 같은 브라우저에서 참가자 상태를 복구한다.
